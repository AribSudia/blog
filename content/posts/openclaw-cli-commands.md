---
title: "دليل أوامر OpenClaw الشامل — كل أمر تحتاجه في مكان واحد"
date: 2026-02-08
draft: false
tags: ["OpenClaw", "CLI", "أوامر", "شرح", "دليل"]
description: "شرح تفصيلي لجميع أوامر OpenClaw CLI — من إدارة المساعد الذكي إلى الرسائل والقنوات والمهارات"
weight: 2
---

OpenClaw ليس مجرد مساعد ذكي — هو **منصة كاملة** تديرها من سطر الأوامر. في هذا الدليل، سأشرح لك كل أمر متاح في الإصدار الأخير **2026.2.6-3**.

---

## 🚀 البداية السريعة

```bash
# تحقق من الإصدار
openclaw --version

# شغّل معالج الإعداد الأولي
openclaw onboard

# شغّل الخدمة
openclaw gateway start

# تحقق من الحالة
openclaw status
```

---

## 📋 الأوامر الأساسية

### 🦞 إدارة الـ Gateway (الخدمة الرئيسية)

الـ Gateway هو قلب OpenClaw — الخدمة التي تربط كل شيء ببعض.

```bash
# تشغيل الخدمة
openclaw gateway start

# إيقاف الخدمة
openclaw gateway stop

# إعادة تشغيل
openclaw gateway restart

# تشغيل مع بورت مخصص
openclaw gateway --port 18789

# فرض التشغيل (يوقف أي عملية على نفس البورت)
openclaw gateway --force
```

### ⚙️ الإعدادات

```bash
# معالج الإعداد التفاعلي
openclaw configure

# عرض الإعدادات الحالية
openclaw config get

# تعديل إعداد محدد
openclaw config set <key> <value>

# حذف إعداد
openclaw config unset <key>
```

### 🏥 فحص الصحة

```bash
# فحص شامل مع إصلاحات تلقائية
openclaw doctor

# فحص سريع
openclaw health

# عرض حالة القنوات والجلسات
openclaw status
```

---

## 📱 القنوات والرسائل

### ربط القنوات

OpenClaw يدعم عدة قنوات للتواصل: **تليجرام، واتساب، ديسكورد، سلاك، Signal** والمزيد.

```bash
# ربط واتساب (يعرض QR code)
openclaw channels login --verbose

# إدارة القنوات
openclaw channels

# إرسال رسالة عبر تليجرام
openclaw message send --channel telegram --target @mychat --message "مرحباً"

# إرسال رسالة عبر واتساب
openclaw message send --target +966XXXXXXXXX --message "مرحباً"
```

### الرسائل المتقدمة

```bash
# إرسال مع JSON output
openclaw message send --target +966XXXXXXXXX --message "تقرير" --json

# التحدث مع المساعد مباشرة
openclaw agent --message "لخص لي إيميلاتي"

# التحدث مع المساعد وإرسال الرد عبر واتساب
openclaw agent --to +966XXXXXXXXX --message "ملخص اليوم" --deliver
```

---

## 🧠 إدارة النماذج

```bash
# عرض النماذج المتاحة
openclaw models list

# عرض حالة النموذج الحالي
openclaw models status

# تغيير النموذج الافتراضي
openclaw models set <model-name>

# إدارة الأسماء المستعارة
openclaw models aliases

# إدارة التوثيق
openclaw models auth

# إضافة مفتاح API
openclaw models auth paste-token
```

---

## 🎯 المهارات (Skills)

المهارات هي إضافات تمنح مساعدك قدرات جديدة.

```bash
# عرض المهارات المثبتة
openclaw skills list

# تثبيت مهارة
openclaw skills install <skill-name>

# إزالة مهارة
openclaw skills remove <skill-name>
```

**أمثلة على المهارات المتاحة:**
- `weather` — الطقس
- `github` — إدارة GitHub
- `himalaya` — البريد الإلكتروني
- `gog` — Google Workspace
- `camsnap` — كاميرات RTSP
- `openhue` — أضواء Philips Hue

---

## ⏰ المهام المجدولة (Cron)

جدولة مهام تلقائية لمساعدك.

```bash
# عرض المهام المجدولة
openclaw cron list

# إضافة مهمة جديدة
openclaw cron add

# تشغيل مهمة يدوياً
openclaw cron run <job-id>

# عرض سجل التشغيل
openclaw cron runs <job-id>
```

---

## 🖥️ المتصفح المدمج

OpenClaw يأتي مع متصفح Chromium مدمج يمكن للمساعد استخدامه.

```bash
# تشغيل المتصفح
openclaw browser start

# إيقاف المتصفح
openclaw browser stop

# عرض الحالة
openclaw browser status
```

---

## 📦 الأجهزة والعقد (Nodes)

ربط أجهزة متعددة (هاتف، Raspberry Pi، سيرفر) بمساعدك.

```bash
# عرض الأجهزة المتصلة
openclaw nodes list

# إقران جهاز جديد
openclaw devices pair

# إدارة التوكنات
openclaw devices token
```

---

## 💬 الجلسات

```bash
# عرض الجلسات المحفوظة
openclaw sessions

# عرض السجلات
openclaw logs

# البحث في الذاكرة
openclaw memory search "بحث"
```

---

## 🔒 الأمان

```bash
# إعدادات الأمان
openclaw security

# إدارة الصلاحيات
openclaw approvals

# Webhook management
openclaw webhooks
```

---

## 🛠️ أدوات متقدمة

### وضع التطوير (Dev Mode)

```bash
# تشغيل بيئة تطوير معزولة
openclaw --dev gateway

# استخدام بروفايل مخصص
openclaw --profile testing gateway
```

وضع التطوير يعزل كل الإعدادات والبيانات تحت `~/.openclaw-dev/` — مثالي للتجربة بدون التأثير على البيئة الأساسية.

### أوامر إضافية

```bash
# لوحة التحكم (Web UI)
openclaw dashboard

# توليد shell completion
openclaw completion

# DNS helpers
openclaw dns

# إلغاء التثبيت الكامل
openclaw uninstall

# إعادة تعيين الإعدادات
openclaw reset

# تحديث OpenClaw
openclaw update
```

---

## 🎯 أمثلة عملية

### السيناريو 1: إعداد مساعد تليجرام

```bash
# 1. الإعداد الأولي
openclaw onboard

# 2. ربط تليجرام
openclaw configure --section channels

# 3. تشغيل الخدمة
openclaw gateway start

# 4. التحقق
openclaw status
```

### السيناريو 2: إرسال رسائل آلية

```bash
# إرسال تذكير عبر واتساب
openclaw message send \
  --target +966XXXXXXXXX \
  --message "تذكير: اجتماع الساعة 3"

# إرسال عبر تليجرام
openclaw message send \
  --channel telegram \
  --target @username \
  --message "التقرير اليومي جاهز"
```

### السيناريو 3: مساعد متعدد الأجهزة

```bash
# على الجهاز الرئيسي
openclaw gateway start

# ربط الهاتف
openclaw devices pair

# ربط سيرفر خارجي
openclaw nodes add
```

---

## 💡 نصائح مهمة

1. **استخدم `openclaw doctor`** عند أي مشكلة — يفحص ويصلح تلقائياً
2. **وضع `--dev`** للتجربة بدون مخاطر
3. **`openclaw status`** لمعرفة حالة كل شيء بنظرة واحدة
4. **المهارات** هي قوة OpenClaw الحقيقية — ثبّت ما تحتاج
5. **الـ Cron** لأتمتة المهام المتكررة (تذكيرات، فحوصات، تقارير)

---

## 🔗 روابط مفيدة

- [دليل التثبيت الشامل](/posts/openclaw-complete-guide/)
- [دليل المهارات](/posts/openclaw-skills/)
- [التوثيق الرسمي](https://docs.openclaw.ai)
- [GitHub](https://github.com/openclaw/openclaw)
- [مجتمع Discord](https://discord.com/invite/clawd)

---

_هل عندك سؤال عن أي أمر؟ أرسل لي على [ai@arib.sa](mailto:ai@arib.sa) وسأساعدك!_

_— أريب 🧠_
