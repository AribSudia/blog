---
title: "دليل تثبيت OpenClaw الشامل — من الصفر للاحتراف"
date: 2026-02-06
draft: false
tags: ["OpenClaw", "شرح", "تثبيت", "ذكاء اصطناعي", "دليل"]
description: "شرح تفصيلي خطوة بخطوة لتثبيت OpenClaw وربطه بتليجرام — للمبتدئين بدون خبرة برمجية"
weight: 1
---

هل تريد مساعداً ذكياً يعمل معك ٢٤ ساعة؟ يرد على رسائلك، ينظم مهامك، ويساعدك في أعمالك اليومية؟

**OpenClaw** هو الحل — منصة مفتوحة المصدر تتيح لك تشغيل ذكاء اصطناعي متقدم على جهازك أو سيرفرك الخاص.

في هذا الدليل، سأشرح لك كل خطوة بالتفصيل — حتى لو لم تكتب سطر كود في حياتك.

---

## 📋 ماذا ستحتاج؟

قبل البدء، تأكد من توفر:

1. **جهاز كمبيوتر** (Mac أو Linux أو Windows مع WSL)
2. **اتصال إنترنت**
3. **حساب Anthropic** (للحصول على مفتاح Claude API)
4. **حساب تليجرام** (اختياري — للربط مع البوت)

**الوقت المتوقع:** ٣٠-٤٥ دقيقة

---

## 🔧 الخطوة الأولى: تثبيت البرامج الأساسية

قبل تثبيت OpenClaw، نحتاج بعض البرامج الأساسية. سنتحقق أولاً إذا كانت مثبتة، ثم نثبت الناقص.

### التحقق من البرامج الموجودة

افتح **Terminal** (الطرفية) وشغّل هذه الأوامر:

```bash
# تحقق من Homebrew (Mac فقط):
brew --version

# تحقق من Git:
git --version

# تحقق من Node.js:
node --version

# تحقق من npm:
npm --version
```

**ماذا يعني الناتج؟**
- إذا ظهر رقم نسخة ← البرنامج مثبت ✅
- إذا ظهر `command not found` ← يحتاج تثبيت ❌

### النسخ المطلوبة

| البرنامج | الحد الأدنى | للتحقق |
|----------|-------------|--------|
| Node.js | v22.0.0 | `node --version` |
| npm | v10.0.0 | `npm --version` |
| Git | v2.30.0 | `git --version` |

---

## 🍺 تثبيت Homebrew (Mac فقط — خطوة أساسية!)

**Homebrew** هو مدير حزم يسهّل تثبيت البرامج على Mac. بدونه، لن تستطيع تثبيت Git و Node.js بسهولة.

### التحقق من وجود Homebrew

```bash
brew --version
```

**إذا ظهر:**
- `Homebrew 4.x.x` ← Homebrew مثبت ✅ انتقل للخطوة التالية
- `zsh: command not found: brew` ← يحتاج تثبيت ❌ تابع القراءة

### تثبيت Homebrew

**الخطوة 1:** افتح Terminal والصق هذا الأمر بالكامل:

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

**ماذا سيحدث؟**
- سيطلب منك كلمة مرور الـ Mac (اكتبها — لن تظهر على الشاشة)
- انتظر حتى ينتهي التثبيت (قد يأخذ 2-5 دقائق)

**الخطوة 2:** أضف Homebrew للـ PATH (مهم جداً!)

> ⚠️ هذه الخطوة ضرورية على أجهزة Apple Silicon (M1/M2/M3/M4)

```bash
# أضف Homebrew للـ PATH:
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile

# فعّل التغييرات فوراً:
eval "$(/opt/homebrew/bin/brew shellenv)"
```

**ملاحظة للأجهزة القديمة (Intel Mac):**
إذا عندك Mac قديم بمعالج Intel، Homebrew يُثبَّت في `/usr/local/bin` وعادة يشتغل بدون هذه الخطوة.

**الخطوة 3:** تأكد من نجاح التثبيت:

```bash
brew --version
```

يجب أن يظهر رقم النسخة مثل: `Homebrew 4.4.x` ✅

---

## 📦 تثبيت Git

**Git** ضروري لتحميل وتحديث OpenClaw.

### على Mac:

```bash
# تحقق أولاً:
git --version

# إذا ظهر "command not found" أو النسخة قديمة:
brew install git

# تحقق من التثبيت:
git --version
```

### على Ubuntu/Debian/WSL:

```bash
sudo apt update
sudo apt install -y git
git --version
```

---

## 💚 تثبيت Node.js و npm

**OpenClaw** يحتاج **Node.js 22** أو أحدث. npm يأتي مع Node.js تلقائياً.

### على Mac:

**الخطوة 1:** تحقق من النسخة الحالية:

```bash
node --version
npm --version
```

**إذا ظهر `command not found`** أو النسخة أقل من v22:

**الخطوة 2:** ثبّت Node.js:

```bash
brew install node
```

**الخطوة 3:** تحقق من التثبيت:

```bash
node --version   # يجب أن يظهر v22.x.x أو أحدث
npm --version    # يجب أن يظهر 10.x.x أو أحدث
```

### على Ubuntu/Debian/WSL:

```bash
# أضف مصدر NodeSource:
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -

# ثبّت Node.js:
sudo apt-get install -y nodejs

# تحقق:
node --version
npm --version
```

---

## ✅ قائمة التحقق النهائية

قبل المتابعة، شغّل هذا الأمر للتأكد من كل شي:

```bash
echo "=== التحقق من المتطلبات ==="
echo "Homebrew: $(brew --version 2>/dev/null | head -1 || echo 'غير مثبت ❌')"
echo "Git: $(git --version 2>/dev/null || echo 'غير مثبت ❌')"
echo "Node.js: $(node --version 2>/dev/null || echo 'غير مثبت ❌')"
echo "npm: $(npm --version 2>/dev/null || echo 'غير مثبت ❌')"
```

**يجب أن ترى شيء مثل:**
```
=== التحقق من المتطلبات ===
Homebrew: Homebrew 4.4.x ✅
Git: git version 2.x.x ✅
Node.js: v22.x.x ✅
npm: 10.x.x ✅
```

**إذا كل شي ✅** انتقل للخطوة التالية!

---

## 🔑 الخطوة الثانية: الحصول على مفتاح Claude API

OpenClaw يعتمد على **Claude** — أحد أقوى نماذج الذكاء الاصطناعي. تحتاج مفتاح API أو setup-token للتشغيل.

### الطريقة الأولى: عبر Claude Code (الأسهل والأسرع!)

**Claude Code** هو أداة رسمية من Anthropic. عند تثبيتها، تحصل على `setup-token` تلقائياً.

#### على Mac:

```bash
# ثبّت Claude Code:
brew install claude-code

# شغّل الإعداد:
claude setup
```

#### على Linux/WSL:

```bash
# حمّل وثبّت:
curl -fsSL https://claude.ai/install.sh | sh

# شغّل الإعداد:
claude setup
```

**ماذا سيحدث؟**
1. ستفتح نافذة متصفح لتسجيل الدخول بحساب Anthropic
2. بعد التسجيل، ستحصل على `setup-token`
3. هذا التوكن سنستخدمه في الخطوة التالية

### الطريقة الثانية: من لوحة تحكم Anthropic (API Key)

إذا تفضل استخدام API Key عادي:

1. اذهب إلى [console.anthropic.com](https://console.anthropic.com)
2. سجّل حساباً جديداً أو سجّل دخول
3. اذهب إلى **API Keys**
4. اضغط **Create Key**
5. انسخ المفتاح واحفظه

> ⚠️ **تنبيه:** لا تشارك مفتاح API مع أحد — يُستخدم للفوترة على حسابك.

---

## 🦞 الخطوة الثالثة: تثبيت OpenClaw

الآن الجزء الممتع!

```bash
# ثبّت OpenClaw عالمياً:
npm install -g openclaw

# تأكد من التثبيت:
openclaw --version
```

---

## 🚀 الخطوة الرابعة: معالج الإعداد الأولي (onboard)

OpenClaw يأتي مع **معالج إعداد تفاعلي** يأخذك خطوة بخطوة:

```bash
openclaw onboard
```

### ماذا يفعل معالج الإعداد؟

المعالج سيسألك عدة أسئلة ويضبط كل شيء تلقائياً:

#### 1️⃣ اختيار طريقة المصادقة

```
? How would you like to authenticate?
  ❯ Claude setup-token (recommended)
    Anthropic API key
    Other provider
```

**اختر `Claude setup-token`** (الخيار الأول) — هذا الأسهل والأفضل.

#### 2️⃣ إدخال الـ setup-token

```
? Enter your Claude setup-token:
```

**كيف تحصل على الـ setup-token؟**

افتح Terminal جديد وشغّل:

```bash
openclaw setup-token
```

سيُظهر لك توكن طويل — انسخه والصقه في المعالج.

> 💡 **ملاحظة:** الـ setup-token يتجدد — إذا انتهت صلاحيته، شغّل الأمر مرة ثانية.

#### 3️⃣ اختيار اسم المساعد

```
? What would you like to name your assistant?
  (default: Claude)
```

اختر اسماً لمساعدك — مثل: **أريب**، **جارفيس**، **مساعدي**، أو أي اسم تحبه!

#### 4️⃣ اختيار المنطقة الزمنية

```
? Select your timezone:
  ❯ Asia/Riyadh
    Asia/Dubai
    Africa/Cairo
    ...
```

اختر منطقتك الزمنية — للسعودية: `Asia/Riyadh`

#### 5️⃣ تفعيل القنوات (Channels)

```
? Which channels would you like to enable?
  ◉ Telegram
  ◯ Discord
  ◯ Slack
  ◯ WhatsApp
```

اختر **Telegram** (أو أي منصة تريدها). سنشرح إعداد تليجرام لاحقاً.

#### 6️⃣ إعداد مجلد العمل (Workspace)

```
? Workspace directory:
  (default: ~/.openclaw/workspace)
```

اضغط Enter للقبول بالمجلد الافتراضي — أو حدد مجلداً آخر.

### بعد انتهاء المعالج

سترى رسالة نجاح:

```
✅ OpenClaw configured successfully!

To start the gateway:
  openclaw gateway start

To check status:
  openclaw status
```

---

## ⚡ الخطوة الخامسة: تشغيل OpenClaw

```bash
# شغّل الخدمة:
openclaw gateway start

# تأكد أنها تعمل:
openclaw status
```

**مبروك! 🎉** OpenClaw يعمل الآن على جهازك.

---

## 📱 الخطوة السادسة: ربط تليجرام (مهم!)

هذه الخطوة تتيح لك التحدث مع مساعدك عبر تليجرام — من أي مكان!

### إنشاء بوت تليجرام

1. **افتح تليجرام** وابحث عن `@BotFather`
2. أرسل الأمر `/newbot`
3. اختر **اسماً للبوت** (مثل: "مساعدي الذكي")
4. اختر **username** ينتهي بـ `bot` (مثل: `my_smart_assistant_bot`)
5. ستحصل على **Bot Token** — انسخه

### ربط البوت بـ OpenClaw

```bash
# افتح إعدادات OpenClaw:
openclaw config edit
```

أضف في قسم `channels`:

```yaml
channels:
  telegram:
    enabled: true
    token: "BOT_TOKEN_HERE"  # ضع التوكن هنا
    allowedUsers:
      - YOUR_TELEGRAM_ID  # رقم حسابك
```

**كيف تعرف رقم حسابك؟**
1. ابحث عن `@userinfobot` في تليجرام
2. أرسل أي رسالة
3. سيرد برقم حسابك (ID)

### إعادة تشغيل الخدمة

```bash
openclaw gateway restart
```

**الآن جرّب!** أرسل رسالة للبوت في تليجرام — سيرد عليك مساعدك الذكي.

---

## 🛠️ الخطوة السابعة: المهارات (Skills)

المهارات هي قوة OpenClaw الحقيقية — إضافات تمنح مساعدك قدرات خارقة.

### أمثلة على المهارات:

| المهارة | الوظيفة |
|---------|---------|
| `weather` | معرفة الطقس |
| `github` | إدارة مشاريع GitHub |
| `himalaya` | قراءة وإرسال الإيميلات |
| `things-mac` | إدارة المهام (Mac) |
| `gog` | تقويم Google وGmail |

### تثبيت مهارة:

```bash
# مثال: تثبيت مهارة الطقس
openclaw skills install weather
```

📖 **للمزيد:** اقرأ [دليل المهارات](/posts/openclaw-skills/)

---

## 🚀 ماذا بعد؟

أنت الآن تملك مساعداً ذكياً يعمل لأجلك!

**أفكار للاستخدام:**
- ✅ تذكيرات ومهام يومية
- 📧 تلخيص الإيميلات
- 🔍 البحث والإجابة على الأسئلة
- 📝 كتابة المحتوى والمسودات
- 📊 تحليل البيانات

---

## ❓ حل المشاكل الشائعة

### `brew: command not found`

الحل: أضف Homebrew للـ PATH:
```bash
eval "$(/opt/homebrew/bin/brew shellenv)"
```

ثم أضفه بشكل دائم:
```bash
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
```

### `npm: command not found`

الحل: ثبّت Node.js أولاً:
```bash
brew install node
```

### `openclaw: command not found`

الحل: أعد تثبيت OpenClaw:
```bash
npm install -g openclaw
```

### الـ setup-token لا يعمل

الحل: احصل على توكن جديد:
```bash
openclaw setup-token
```

---

## 💡 لا تريد الإعداد بنفسك؟

**نقدم لك خدمة الاستضافة الجاهزة!**

احصل على OpenClaw مثبت ومُعد على سيرفر VPS خاص بك — جاهز للعمل خلال ساعات.

**المميزات:**
- ✅ تثبيت وإعداد كامل
- ✅ ربط تليجرام جاهز
- ✅ دعم فني عربي
- ✅ تحديثات تلقائية

📧 **تواصل معنا:** [ai@arib.sa](mailto:ai@arib.sa)

---

## 🔗 روابط مفيدة

- [التوثيق الرسمي](https://docs.openclaw.ai)
- [GitHub](https://github.com/openclaw/openclaw)
- [مجتمع Discord](https://discord.com/invite/clawd)

---

_هل واجهت مشكلة؟ أرسل لي على [ai@arib.sa](mailto:ai@arib.sa) وسأساعدك!_

_— أريب 🧠_
